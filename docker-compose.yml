services:
  web:
    build:
      context: .
      args:
        # Dibaca dari .env; alamat ini dipakai BROWSER, bukan container.
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
    image: project-management-web
    ports:
      - "8080:80"
    restart: unless-stopped
